import { MarkdownEditorDirective } from './mde.directive';

describe('AceDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkdownEditorDirective(null);
    expect(directive).toBeTruthy();
  });
});
